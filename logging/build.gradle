dependencies {
    implementation project(':api')
    implementation project(':core')
}

jar {
    manifest {
        attributes 'Implementation-Title': 'Jobserv Logging',
                'Implementation-Version': archiveVersion
    }
}