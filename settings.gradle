rootProject.name = 'jobserv'

['annotation', 'api', 'common', 'core', 'docs', 'logging', 'monitoring', 'performance', 'sample'].each { module ->
    include ":$module"
    project(":$module").projectDir = file(module)
}